version: "2"
syncs:
  bom-osx-sync:
    src: './src/bom'
    sync_strategy: 'native_osx'
    sync_excludes: ['generated', 'pub','var','.git','.idea']
    monit_enable: true

    # increase tolerance of high cpu if regular syncing uses a lot cpu
    monit_interval: 10
    monit_high_cpu_cycles: 6